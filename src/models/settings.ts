export interface Settings {
    isOpenLecture: boolean;
    lectureSpeed: number;
    audioSpeed: number;
    fontSize: string;
    theme: string;
}
export interface SettingData {
    fontSize: string;
}